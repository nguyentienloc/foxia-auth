enum Operator {
    ADD = '+',
    SUBTRACT = '-',
    MULTIPLY = '*',
    DIVIDE = '/',
    MODULUS = '%',
    EXPONENTIATE = '**',
    EQUAL = '==',
    STRICT_EQUAL = '===',
    NOT_EQUAL = '!=',
    STRICT_NOT_EQUAL = '!==',
    GREATER_THAN = '>',
    GREATER_THAN_OR_EQUAL = '>=',
    LESS_THAN = '<',
    LESS_THAN_OR_EQUAL = '<=',
    AND = '&&',
    OR = '||',
    NOT = '!',
    BITWISE_AND = '&',
    BITWISE_OR = '|',
    BITWISE_XOR = '^',
    BITWISE_NOT = '~',
    LEFT_SHIFT = '<<',
    RIGHT_SHIFT = '>>',
    UNSIGNED_RIGHT_SHIFT = '>>>'
  }
  
  export default Operator;